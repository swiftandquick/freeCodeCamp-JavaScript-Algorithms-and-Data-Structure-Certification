function alphabeticalOrder(arr) {
  // Only change code below this line
  // Sort in malphetical order.  
  return arr.sort();
  // Only change code above this line
}

console.log(alphabeticalOrder(["a", "d", "c", "a", "z", "g"]));